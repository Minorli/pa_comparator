DELIMITER $$

CREATE OR REPLACE PROCEDURE         SP_ARCHIVE_AUDIT (
    p_cleanup_days IN NUMBER
) AS
BEGIN
    INSERT INTO ORA_LOG.AUDIT_ARCHIVE
    SELECT *
      FROM ORA_LOG.AUDIT_EVENTS
     WHERE EVENT_TS < SYSDATE - p_cleanup_days;
END;
$$

