DELIMITER $$

CREATE OR REPLACE FUNCTION         FN_LAST_AUDIT_ID RETURN NUMBER IS
    v_id NUMBER;
BEGIN
    SELECT NVL(MAX(EVENT_ID), 0) INTO v_id FROM ORA_LOG.AUDIT_EVENTS;
    RETURN v_id;
END;
$$

