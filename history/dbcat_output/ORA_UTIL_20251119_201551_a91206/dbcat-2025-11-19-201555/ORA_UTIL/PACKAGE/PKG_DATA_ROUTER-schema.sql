DELIMITER $$

CREATE OR REPLACE PACKAGE          PKG_DATA_ROUTER AS
    PROCEDURE REFRESH_REGION_CACHE;
    FUNCTION COUNT_ORDERS_BY_REGION(p_region_code VARCHAR2) RETURN NUMBER;
END PKG_DATA_ROUTER;
$$

